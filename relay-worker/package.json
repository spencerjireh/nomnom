{
  "name": "nomnom-relay",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that brokers encrypted file transfers for nomnom.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.16.11",
    "@cloudflare/workers-types": "^4.20250501.0",
    "typescript": "^5.5.0",
    "vitest": "^4.1.8",
    "wrangler": "^4.95.0"
  }
}
